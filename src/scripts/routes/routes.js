import Home from '../views/pages/home';
import Aboutus from '../views/pages/aboutus';

const routes = {
  '/': Home, // default page
  '/home': Home,
  '/aboutus': Aboutus,
};

export default routes;
